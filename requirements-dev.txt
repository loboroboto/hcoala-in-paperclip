# Dev/test dependencies for CI (offline). Runtime deps (httpx, pyyaml) are baked into the
# image via docker/Dockerfile; this file is for `pytest tests/` + py_compile in CI.
pytest>=8
httpx>=0.27
pyyaml>=6
